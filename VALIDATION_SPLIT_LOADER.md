# Split-shell runtime validation

This marker commit exists to trigger the complete regression suite after the generated loader switched from the monolithic `viewer-source.html` runtime dependency to `viewer-shell.html` + `viewer-legacy-scripts.html`.

No clinical DICOM or PHI is stored in the repository.
